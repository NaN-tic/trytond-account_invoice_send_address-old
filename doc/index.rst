Account Invoice Send Address
############################

The Account Invoice Send Address allows to define a diferent address for
address which the invoice will be sended to. Party addresses can be marked
as send_invoice and then they will be used for sending the invoices by default.
